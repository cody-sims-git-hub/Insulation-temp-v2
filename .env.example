# Public production URL — used for canonical tags, sitemap.xml, robots.txt,
# Open Graph URLs, and JSON-LD structured data. No trailing slash.
# Set this to the site's real domain in your hosting/env config.
NEXT_PUBLIC_SITE_URL=https://www.aplusinsulation.com
